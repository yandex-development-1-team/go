version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

# дефолтный набор линтеров (errcheck, govet, gofmt, staticcheck и др.)
# при необходимости: linters.enable / linters.disable
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
